[phases.install]
cmds = [
  'cd frontend && npm ci',
  'cd backend && npm ci'
]

[phases.build]
cmds = [
  'cd frontend && npm run build',
  'cd backend && npm run build'
]

[start]
cmd = 'node backend/dist/index.js'
